<h4><%= "Last #{pluralize(games.count, 'game')}" %></h4>
<table class="table table-striped table-responsive-md">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Date / Time</th>
      <th scope="col">Winner</th>
      <th class="d-none d-sm-table-cell" scope="col">Buy In</th>
      <th scope="col"># of Players</th>
      <th scope="col">Pot Size</th>
    </tr>
  </thead>
  <tbody>
    <% if games.any? %>
      <%= render partial: 'shared/game_row', collection: games, as: :game %>
    <% else %>
      <tr class="season-show__games-table">
        <td colspan="5">No games just yet, check back later!</td>
      </tr>
    <% end %>
  </tbody>
</table>
